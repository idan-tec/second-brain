# << branch >> — open

> **This file is what is still open. It is never the history of how it got opened.** A settled item leaves one line saying it is settled and where the reasoning went; the reasoning itself lives in `decisions/DECISIONS.md`.
> **Ceiling: about 1KB per open item.** Past that, what grew is background that has become a narrative, and its home is the log. An open list nobody can finish reading has stopped being a list.
> **This branch's list only.** Ids live in the `<< ABC >>-O*` namespace so they never collide with the brain's `O*`, or with the numbering an imported branch brought with it.
> **What is open at brain level — ownership, money, anything crossing branches — belongs in [`../../decisions/OPEN.md`](../../decisions/OPEN.md)**, and the map at the top of that file is the roll-up of every list, including this one. **Add the row there in the same action that creates this file**, or this list is invisible from the only place that answers *"what is open across everything"*.
> **This file was born with its first item and not before.** An open list created empty teaches the owner that there is nothing open here, which is a claim no empty file has earned.
> **Every item carries a written recommendation and a default.** An open question with no recommendation is handing responsibility back, not asking for a decision — and it is the reason lists like this stop being read.
> **When an item here is settled and it touched money, ownership, or anyone outside this folder, its line also goes into the root [`DECISIONS.md`](../../decisions/DECISIONS.md).** The decision log stays single on purpose: the open lists are working surfaces, read by whoever is on this one branch, while the log is the history of the whole folder and gets read end to end.

<< If the whole list sits under a standing owner ruling — "there is nothing in writing and I do not want there to be",
   "do not raise this again until the quarter closes" — that ruling goes here, once, above the items, with its date and in
   their words. It is the frame every item below is read through, and repeating it per item is how it drifts.
   A recommendation that was overruled stays on the record. It was not disproved, it was decided against, and deleting it
   turns a decision the owner made into a decision nobody remembers making. >>

---

### << ABC >>-O1 · << the question, in one line >>
- **Opened:** << YYYY-MM-DD >>
- **Background:** << what makes this a real fork, and what is actually at stake either way >>
- **Recommendation:** << a clear position, with the reasoning. Not a survey of options. >>
- **Default if not decided by << date >>:** << what happens automatically, logged in DECISIONS.md as `assumed` >>

### << ABC >>-O2 · << ... >>

<< same shape >>

---

**Items that pre-date this file stay where they were opened.** Moving an id breaks every reference to it that was ever written down, and the references are in files nobody is going to re-read. Note them in the map instead, and migrate them only as a deliberate, logged decision.
