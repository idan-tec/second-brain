Rules for this branch: `./README.md` — read it now.

The rulebook for this whole folder is the root `CLAUDE.md`, two levels up. Claude Code loads it automatically as a parent; if your tool does not, read it first. It governs, and a branch rule may only add or tighten, never contradict it.

(This file is an adapter. It holds no rules of its own, and it changes only by a logged owner decision.)
